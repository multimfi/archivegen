// TODO: ignore/user/group/mode mask.

// directory
d <dst> <perm> <user> <group>

// symlink
// /dst -> src
l <dst> <src> <user> <group>

// file from disk
f <src> <dst> <perm> <user> <group>

// recursive
// src prefix is stripped from dst
R <src> <dst>

// createfile
c <dst> <mode> <user> <group> <data...>

// elf
L <elf> <dst> <perm> <user> <group>
